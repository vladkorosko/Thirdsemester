class Solution {
public:
    bool isPalindrome(int x) 
    {
        string str = to_string(x);
        string str1 = str;
        reverse(str.begin(), str.end());
        return str == str1;
    }
};
